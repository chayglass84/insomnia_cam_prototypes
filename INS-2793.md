# INS-2793 — Simplify environment picking & configuration

## Problem

Picking and configuring environments is too complicated to discover today. This is a **UX-only** change: no behavior changes to how environments actually work (scoping, inheritance, sync, private vs shared) — only how they're presented and how discoverable configuration actions are.

## Background (current behavior, unchanged)

- **Project-level environments**: stored as hidden `scope: 'environment'` workspaces at the project level, usable by any collection in the project. Each can itself have shared/private sub-environments.
- **Collection-level environments**: stored inside the collection (workspace), usable anywhere in that collection. Has a base environment + shared/private sub-environments.
- **Folder-level**: not a real "environment" — a `RequestGroup` has an `environment`/`kvPairData` field edited via an "Environment" tab in the folder pane. **Out of scope for this task.**
- Inheritance: collection overrides folder overrides project (existing behavior, unchanged).
- Sub-environments: "shared" (`isPrivate: false`, synced/exported) vs "private" (`isPrivate: true`, never synced/exported).
- Secret env vars / raw-JSON vs table (KV) toggle: exists today (`EnvironmentKvPairDataType.SECRET`, `useToggleEnvironmentType`). **Out of scope for this task.**

## Current implementation (for reference)

- `packages/insomnia/src/ui/components/environment-picker.tsx` — `EnvironmentPicker`. Single button in `workspace-pane-header.tsx` opens a popover containing: a "Project Environments" panel (a searchable `ComboBox` to pick *which* project-level file, then a separate `ListBox` below it to pick a specific sub-environment within that file — two nested pickers) and a "Collection Environments" panel (one flat `ListBox` of base + sub-environments).
- `packages/insomnia/src/ui/components/modals/workspace-environments-edit-modal.tsx` — `WorkspaceEnvironmentsEditModal`, the full editor opened via the collection panel's edit icon. Had an always-visible "+" icon next to the base environment row, but it only revealed the two add options ("Shared environment" / "Private environment") inside a dropdown menu — the low-discoverability add flow being fixed.

## What's changing (this task)

1. **Project-level dropdown**: collapse the current two-level picker (search combobox → sub-environment list) into **one flattened dropdown** listing every project environment file with its sub-environments nested/indented underneath. One tooltip on the section explaining scope. Each **top-level file row** gets its own edit icon that navigates to that file's environment route (`/organization/:orgId/project/:projectId/workspace/:workspaceId/environment`) — same navigation the current gear button uses, just per-row instead of only for the currently-selected file.
2. **Collection-level dropdown**: keep the existing flat base+sub-environment list; add a tooltip explaining scope. Single edit icon next to the dropdown (not per-row) opens `WorkspaceEnvironmentsEditModal`, same as today.
3. **Popover/trigger structure**: unchanged — still one trigger button in the header opening a popover; only the *contents* of the popover are simplified per above.
4. **`WorkspaceEnvironmentsEditModal` discoverability**: replace the hidden hover-only "+" icon/menu with two explicit, always-visible buttons: **"Add Sub Environment"** and **"Add Private Sub Environment"**. Same underlying create action as today (`isPrivate: false` / `true`), just surfaced directly instead of hidden behind a menu.

## Explicitly out of scope

- Folder-level "Environment" tab (request group state).
- Secret environment variables / raw-JSON vs table (KV) toggle.
- Any change to inheritance, sync, or storage behavior.
- Moving the tab-bar navigation model (edit-as-tab keeps using existing full-page route navigation, not real tab-bar integration).

## Follow-up: environment picker rebuilt as two independent dropdowns

Reworked `EnvironmentPicker` (`packages/insomnia/src/ui/components/environment-picker.tsx`) from one button opening a popover with two side-by-side panels into **two separate dropdown triggers** side by side in the header: "Select a Project Environment" and "Select a Collection Environment", each its own `DialogTrigger`/`Popover`/`ListBox`. Each trigger is a simplified pill (icon + name + caret), tooltip explains scope on hover. Collection dropdown keeps its edit icon in the popover header (opens `WorkspaceEnvironmentsEditModal`); project dropdown keeps per-row edit icons on base environment rows. Verified visually via a throwaway Playwright screenshot script (not committed) — renders correctly, selection works, edit icon opens the modal.

**Breaking change, deliberately not fixed (prototype, exploratory design):** the old single "Manage Environments" trigger button is gone, replaced by the two new buttons above. ~15 files under `packages/insomnia-smoke-test/tests/` (e.g. `focus-and-keyboard.test.ts`, `environment-editor-interactions.test.ts`, `insomnia-vault.test.ts`, `global-environments.test.ts`, `pre-request-script-features.test.ts`, `after-response-script-features.test.ts`, `url-bar-undo.test.ts`, `key-value-editor-blank-row.test.ts`, `local-to-cloud-projects.test.ts`) click `getByRole('button', { name: 'Manage Environments' })` / `getByLabel('Manage Environments')` and then select an option directly — these will fail until updated to target the new per-level buttons. Left broken on purpose per user direction; fix once this design is locked in, not before.

### Follow-up: add-project-environment button + inheritance tooltip

- Project dropdown header renamed "Project Environment" → **"Project Environments"** (plural, since a project can hold more than one).
- Added a **"+" button** ("Add Project Environment") in the project dropdown header that opens the existing `NewWorkspaceModal` with `scope: "environment"` (same modal/flow as "Create in project" → "Environment" elsewhere in the app), scoped to `activeProject`, using `DEFAULT_STORAGE_RULES` from `~/ui/organization-utils` since the picker has no route-level storage-rules loader data and the mock-server-only checks in that modal don't apply to the environment scope.
- Both dropdown headers now share the same right-side icon order: the level-specific action first (edit icon for Collection, add icon for Project), then an inheritance-explainer info icon (`circle-info`) furthest right, tooltip: "Values inherit down and can be overridden: collection environment values override folder values, which override project environment values."
- Verified visually via a throwaway Playwright screenshot script (not committed): both headers render with matching icon layout, the "+" opens the create-environment modal correctly.

## Known shortcuts / not production-ready

- No fakes/shortcuts were needed — this reused existing fetchers/actions/routes (`useEnvironmentSetActiveGlobalActionFetcher`, `useEnvironmentCreateActionFetcher`, the existing `/environment` route) and only changed presentation.
- Two smoke tests (`focus-and-keyboard.test.ts`, `environment-editor-interactions.test.ts`) referenced the old "+" dropdown (`CreateEnvironmentDropdown` test id + "Shared Environment" menu item) to create a collection sub-environment; updated both to click the new `AddSubEnvironment` button directly.
- **Follow-up done:** the project/global environment full-page editor (`packages/insomnia/src/routes/organization.$organizationId.project.$projectId.workspace.$workspaceId.environment.tsx`) had the same hidden-behind-a-menu "+" button (test id `CreateEnvironmentDropdown`). Replaced it with the same two explicit "Add Sub Environment" / "Add Private Sub Environment" buttons (stacked, padded, text-sm, bordered) above the environment list, removed the per-row menu trigger and its now-unused `CreateEnvironmentDropdown` test id, and reworded the action descriptions to match the collection-modal tooltips. Verified visually via a throwaway Playwright screenshot script (not committed). Note: `global-environments.test.ts` ("create a new global environment") still references `CreateEnvironmentDropdown` and `'Private environment'` text — left broken per prototype scope, same as the other picker-related smoke test breakage above.
